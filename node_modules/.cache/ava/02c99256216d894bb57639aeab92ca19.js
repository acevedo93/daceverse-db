'use strict';

let db = null;

const test = require('ava');

const config = {
  logging: function () {}
};
test.beforeEach(async () => {
  const setupDatabase = require('../');

  db = await setupDatabase(config);
});
test('make it pass', t => {
  t.pass();
});
test('Agent', t => {
  console.log(db);
  t.truthy(db.Agent, 'Agent service chould exist');
});
//# sourceMappingURL=/home/david/code/daceverse/daceverse-db/node_modules/.cache/ava/02c99256216d894bb57639aeab92ca19.js.map