'use strict';

let db = null;

const test = require('ava');

const proxyquire = require('proxyquire');

const sinon = require('sinon');

let sandbox = null;
const config = {
  // este objeto de configuracion se le pasa a la base solo como base
  // la configuracion de la base de prueba se genera en index validando que tiene esa config
  logging: function () {}
};
test.beforeEach(async () => {
  //obtenemos el modulo de configuracion de bbase de datos
  const setupDatabase = require('../');

  db = await setupDatabase(config);
});
test('make it pass', t => {
  t.pass();
});
test('metric exist', t => {
  t.truthy(db.Metric, 'Metric service should exist');
});
//# sourceMappingURL=/home/david/code/daceverse/daceverse-db/node_modules/.cache/ava/1126557b4052f876443b74128063dd14.js.map